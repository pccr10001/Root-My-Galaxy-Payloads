# Galaxy S24 China SM-S9210 / S9210ZHS5CZB2 rebuild record

This profile was rebuilt from the raw AP and BL inputs without using the prior
target header, fingerprint table, documentation, or binary artifacts.

## Firmware identity

```text
model: SM-S9210
device: e1q
AP/PDA: S9210ZHS5CZB2
fingerprint: samsung/e1qzhx/e1q:16/BP2A.250605.031.A3/S9210ZHS5CZB2:user/release-keys
kernel release: 6.1.128-android14-11-31999054-abS9210ZHS5CZB2
page size: 4096
```

The connected device reported every value above. The raw kernel is 36,956,672
bytes with SHA-256
`445b17ee0b7768343d587227cb05f00054ea6adf161d1f6d9bc7188d258b6b45`.

## Fresh recovery

`vmlinux-to-elf` recovered an AArch64 ELF at
`0xffffffc008000000`. Its SHA-256 is
`0f88fa326b34992984da20be4a70737b00d7556c73ed098a97a1b2fa0521b635`.
The uniquely validated raw BTF interval is `[0x1759e0c, 0x1cf0646)`;
the extracted BTF SHA-256 is
`299d927ede27aeb759dac1c0836366dead4400fe544e06b53a06f34c98c403a9`.

The recovered BTF confirms compact `struct rt_mutex_waiter` layout
(`0x58` bytes), 0x110-byte `file_operations`, 0x12c0-byte `task_struct`,
0x40-byte `page`, 0x3c0-byte `mm_struct`, and 0x158-byte
`skb_shared_info`. Every symbol and layout constant in the target header is
derived from these recovered files.

The target runtime reports trace event `sched:sched_blocked_reason` ID 106.
The return instruction after `worker_thread`'s `bl schedule` is
`0x000da848` relative to the image base.

## Qualcomm handoff and P0

The exact `abl.elf.lz4` was decompressed to SHA-256
`d2bf34e7e5b17d4a2e7401ea439cb6617ab2c4fe98d74ab7c87d71167ccf5c4a`.
Its UEFI GUID-defined LZMA section contains the ARM64 LinuxLoader PE image.
LinuxLoader selects the ARM64 load constants `0x00080000` and `0x05600000`,
then adds the selected load offset to the lowest RAM base. With RAM at
`0x80000000` and the Image `text_offset` equal to zero, the profile uses:

```c
#define P0_PHYS_OFFSET 0x80000000ULL
#define P0_KERNEL_PHYS_LOAD 0x80080000ULL
```

`p0_fingerprint.h` was regenerated at probe offset `0x1f0000`. It contains
all 32 64-KiB slide candidates and was independently read back from the raw
Image: 256 of 256 qwords match.

## Artifacts

```text
artifacts/e1q-S9210ZHS5CZB2/cve-2026-43499-app.so
size: 104128
SHA-256: 6c025165ecf24ffe22f2413d5ea0d239aae88e84acca4dbc29fd22f52ed49276

artifacts/e1q-S9210ZHS5CZB2/cve-2026-43499-root
size: 27072
SHA-256: 54894e9bfa80fc36cfa03bd4ec4279e1d56eccc9719c9ee395b64ffd6792866b
```

The app payload and helper compile with Android NDK r29. The app artifact is
listed in `support/targets-v3.json` for manual testing. Hardware execution and
a matching KernelSU module have not yet been validated; neither is claimed by
this record.
